package io.reflectoring.buckpal.account.domain;

import io.reflectoring.buckpal.generic.EntityId;

public class ActivityId extends EntityId<String> {
    public ActivityId(String id) {
        super(id);
    }
}
